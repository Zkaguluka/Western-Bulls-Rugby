$(

	$("#homepageSubscribeButton").click(function() {
		var userName, email;
		userName= $("#homepageUserName").val();
		email= $("#homepageUserEmail").val();
		alert(userName);

		if (userName.length==0 && email.length==0) {
			 
		}

	});

);
